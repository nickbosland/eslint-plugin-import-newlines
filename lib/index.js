"use strict";module.exports={rules:{"imports-multiline":{create:function(e){return{ImportDeclaration(t){if(t.specifiers.length<=4)return;let r=t.specifiers[0];for(let i=1;i<t.specifiers.length;i++){const s=t.specifiers[i];if("ImportDefaultSpecifier"!==r.type&&s.loc.start.line===r.loc.end.line)return void e.report({node:t,message:"Imports must be in multiline if there are more than 4 elements."});r=s}}}}}}};